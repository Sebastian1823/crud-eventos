<nav class="navbar navbar-expand-lg navbar-glass shadow-sm">
  <div class="container-fluid px-4">
    <a class="navbar-brand fw-bold fs-5 d-flex align-items-center gap-2 text-white" href="/dashboard">
      <div class="brand-icon">
        <i class="bi bi-calendar-event-fill text-white"></i>
      </div>
      <span>Eventos<span style="color:#a5b4fc">Pro</span></span>
    </a>

    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
            style="color:rgba(255,255,255,.7)">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav me-auto ms-3 gap-1">
        <li class="nav-item">
          <a class="nav-link nav-link-custom d-flex align-items-center gap-2 <%= locals.title && title.includes('Dashboard') ? 'active' : '' %>"
             href="/dashboard">
            <i class="bi bi-speedometer2"></i> Dashboard
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-link-custom d-flex align-items-center gap-2 <%= locals.title && title.includes('Evento') ? 'active' : '' %>"
             href="/eventos">
            <i class="bi bi-calendar3"></i> Eventos
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-link-custom d-flex align-items-center gap-2 <%= locals.title && title.includes('Organizador') ? 'active' : '' %>"
             href="/organizadores">
            <i class="bi bi-people-fill"></i> Organizadores
          </a>
        </li>
      </ul>

      <% if (user) { %>
      <div class="d-flex align-items-center gap-3">
        <div class="user-badge d-flex align-items-center gap-2">
          <div class="avatar-sm">
            <i class="bi bi-person-circle"></i>
          </div>
          <span class="text-white-75 small fw-medium"><%= user.username %></span>
        </div>
        <a href="/logout" class="btn btn-sm d-flex align-items-center gap-1"
           style="background:rgba(255,255,255,.1);color:#fff;border:1px solid rgba(255,255,255,.2)">
          <i class="bi bi-box-arrow-right"></i> Salir
        </a>
      </div>
      <% } %>
    </div>
  </div>
</nav>
